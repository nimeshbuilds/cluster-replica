---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: clusterreplicas.replica.nimeshbuilds.dev
spec:
  group: replica.nimeshbuilds.dev
  names:
    kind: ClusterReplica
    listKind: ClusterReplicaList
    plural: clusterreplicas
    shortNames:
    - crpl
    singular: clusterreplica
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.runtime.releaseName
      name: Runtime
      type: string
    - jsonPath: .status.expiresAt
      name: Expires
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ClusterReplicaSpec describes one immutable, administrator-granted replica.
              Create a new request to change its immutable runtime or lifetime.
            properties:
              approval:
                default: Automatic
                description: GrantRef delegates source capabilities to this destination
                  namespace.
                enum:
                - Automatic
                - Manual
                type: string
              cleanupPolicy:
                description: CleanupPolicy selects the legacy Helm-only lifecycle
                  or full owned-resource cleanup.
                enum:
                - HelmReleaseOnly
                - DeleteOwned
                type: string
              grantRef:
                type: string
              profile:
                description: Profile selects an exact, experimental runtime configuration.
                  No profile is certified yet.
                enum:
                - vcluster-0.37.1-lab
                type: string
              replication:
                properties:
                  checks:
                    description: Additional readiness checks for custom resources.
                    items:
                      properties:
                        apiVersion:
                          type: string
                        condition:
                          description: Use either a condition or a dot-separated status
                            field and expected value.
                          type: string
                        equals:
                          type: string
                        field:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                      required:
                      - apiVersion
                      - kind
                      - name
                      type: object
                    type: array
                  data:
                    default: None
                    description: EmptyVolumes creates fresh storage; it does not copy
                      source data.
                    enum:
                    - None
                    - EmptyVolumes
                    type: string
                  exclude:
                    items:
                      properties:
                        groups:
                          items:
                            type: string
                          type: array
                        kinds:
                          items:
                            type: string
                          type: array
                        labelSelector:
                          description: |-
                            A label selector is a label query over a set of resources. The result of matchLabels and
                            matchExpressions are ANDed. An empty label selector matches all objects. A null
                            label selector matches no objects.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        names:
                          items:
                            type: string
                          type: array
                        namespaces:
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  helmOverrides:
                    items:
                      properties:
                        name:
                          type: string
                        namespace:
                          type: string
                        values:
                          x-kubernetes-preserve-unknown-fields: true
                      required:
                      - name
                      - namespace
                      - values
                      type: object
                    type: array
                  helmReleases:
                    items:
                      properties:
                        name:
                          type: string
                        namespace:
                          type: string
                      required:
                      - name
                      - namespace
                      type: object
                    type: array
                  include:
                    items:
                      properties:
                        groups:
                          items:
                            type: string
                          type: array
                        kinds:
                          items:
                            type: string
                          type: array
                        labelSelector:
                          description: |-
                            A label selector is a label query over a set of resources. The result of matchLabels and
                            matchExpressions are ANDed. An empty label selector matches all objects. A null
                            label selector matches no objects.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        names:
                          items:
                            type: string
                          type: array
                        namespaces:
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  namespaceMap:
                    additionalProperties:
                      type: string
                    type: object
                  namespaces:
                    description: Empty means all source namespaces explicitly delegated
                      by the grant.
                    items:
                      type: string
                    type: array
                  patches:
                    items:
                      properties:
                        apiVersion:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                        patch:
                          x-kubernetes-preserve-unknown-fields: true
                      required:
                      - apiVersion
                      - kind
                      - name
                      - patch
                      type: object
                    type: array
                  secrets:
                    default: None
                    enum:
                    - None
                    - Snapshot
                    - Follow
                    type: string
                  storageClassMap:
                    additionalProperties:
                      type: string
                    type: object
                type: object
              target:
                properties:
                  existingRef:
                    description: Name of an administrator-defined existing target
                      in the grant.
                    type: string
                  provider:
                    default: auto
                    description: auto honors the grant's configured provider; it never
                      bypasses Platform.
                    enum:
                    - auto
                    - helm
                    - existing
                    - platform
                    type: string
                type: object
              ttl:
                description: TTL starts at the resource's creation time, including
                  provisioning time.
                pattern: ^([1-9][0-9]{0,3}m|[1-9][0-9]{0,2}h)$
                type: string
                x-kubernetes-validations:
                - message: ttl must be between 5 minutes and 168 hours
                  rule: duration(self) >= duration('5m') && duration(self) <= duration('168h')
            required:
            - cleanupPolicy
            - profile
            - ttl
            type: object
            x-kubernetes-validations:
            - message: spec is immutable; create a new ClusterReplica
              rule: self == oldSelf
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              driftCount:
                format: int32
                type: integer
              expiresAt:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              plan:
                properties:
                  appliedCount:
                    format: int32
                    type: integer
                  capturedAt:
                    format: date-time
                    type: string
                  message:
                    description: Only object identity and reasons are exposed; capture
                      payloads stay encrypted.
                    type: string
                  objectCount:
                    format: int32
                    type: integer
                  packageCount:
                    format: int32
                    type: integer
                  resources:
                    items:
                      properties:
                        apiVersion:
                          type: string
                        dependencies:
                          items:
                            type: string
                          type: array
                        kind:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                        sourceNamespace:
                          type: string
                      required:
                      - apiVersion
                      - kind
                      - name
                      type: object
                    type: array
                  revision:
                    type: string
                required:
                - capturedAt
                - objectCount
                - packageCount
                - revision
                type: object
              runtime:
                description: RuntimeReference is persisted before installation. It
                  contains no credentials.
                properties:
                  chartSHA256:
                    type: string
                  chartVersion:
                    type: string
                  guestKubernetesVersion:
                    type: string
                  profile:
                    type: string
                  releaseName:
                    type: string
                required:
                - chartSHA256
                - chartVersion
                - guestKubernetesVersion
                - profile
                - releaseName
                type: object
              sourceVersion:
                type: string
              targetVersion:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
