# Generated by make manifests. Edit charts/replicove, then regenerate.
---
# Source: replicove/templates/bootstrap.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: replicove-bootstrap
  namespace: replicove-system
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: replicove-bootstrap
  namespace: replicove-system
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
rules:
  - apiGroups: [""]
    resources: [secrets]
    verbs: [get, list, create]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: replicove-bootstrap
  namespace: replicove-system
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: replicove-bootstrap
subjects:
  - kind: ServiceAccount
    name: replicove-bootstrap
    namespace: replicove-system
---
apiVersion: batch/v1
kind: Job
metadata:
  name: replicove-bootstrap
  namespace: replicove-system
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
spec:
  backoffLimit: 3
  activeDeadlineSeconds: 300
  template:
    metadata:
      labels:
        replicove.nimeshbuilds.dev/infrastructure: "true"
    spec:
      restartPolicy: OnFailure
      serviceAccountName: replicove-bootstrap
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: bootstrap
          image: "ghcr.io/nimeshbuilds/replicove:0.2.0-alpha.2"
          imagePullPolicy: IfNotPresent
          args:
            - --bootstrap-state-key
            - --state-namespace=replicove-system
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 128Mi
---
# Source: replicove/templates/operator.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
  name: replicove
  namespace: replicove-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
  name: replicove
  namespace: replicove-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: replicove
  template:
    metadata:
      labels:
        app.kubernetes.io/instance: replicove
    spec:
      serviceAccountName: replicove
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: operator
          image: "ghcr.io/nimeshbuilds/replicove:0.2.0-alpha.2"
          imagePullPolicy: IfNotPresent
          args:
            - --watch-namespace=replica-lab
            - --state-namespace=replicove-system
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
          ports:
            - name: probes
              containerPort: 8081
          readinessProbe:
            httpGet:
              path: /readyz
              port: probes
          livenessProbe:
            httpGet:
              path: /healthz
              port: probes
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 100m
              memory: 256Mi
          volumeMounts:
            - name: temporary
              mountPath: /tmp
      volumes:
        - name: temporary
          emptyDir:
            medium: Memory
            sizeLimit: 64Mi
---
# Source: replicove/templates/rbac.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
  name: replicove-state
  namespace: replicove-system
rules:
  - apiGroups: [""]
    resources: [secrets]
    verbs: [get, list, create, update, patch, delete]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
  name: replicove-state
  namespace: replicove-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: replicove-state
subjects:
  - kind: ServiceAccount
    name: replicove
    namespace: replicove-system
---
# The pinned vCluster Role plus installation, inventory, and controller rights.
# No bind/escalate bypass: Kubernetes checks each Role/RoleBinding we create.
# TestPinnedChartContractRBAC checks this against both upstream profiles.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
  name: replicove-runtime
  namespace: replica-lab
rules:
  - apiGroups: [""]
    resources: [configmaps, secrets, services, endpoints, pods, pods/attach, pods/portforward, pods/exec, persistentvolumeclaims]
    verbs: [get, list, watch, create, update, patch, delete]
  - apiGroups: [""]
    resources: [pods/status, pods/ephemeralcontainers, pods/resize]
    verbs: [update, patch]
  - apiGroups: [""]
    resources: [pods/log]
    verbs: [get, list, watch]
  - apiGroups: [""]
    resources: [serviceaccounts]
    verbs: [get, list, watch, create, update, patch, delete]
  - apiGroups: ["", events.k8s.io]
    resources: [events]
    verbs: [get, list, watch, create, patch]
  - apiGroups: [apps]
    resources: [deployments, statefulsets]
    verbs: [get, list, watch, create, update, patch, delete]
  - apiGroups: [apps]
    resources: [replicasets]
    verbs: [get, list, watch, update, patch, delete]
  - apiGroups: [apps]
    resources: [daemonsets]
    verbs: [get, list, delete]
  - apiGroups: [batch]
    resources: [jobs, cronjobs]
    verbs: [get, list, delete]
  - apiGroups: [discovery.k8s.io]
    resources: [endpointslices]
    verbs: [get, list, watch, create, update, patch, delete]
  - apiGroups: [coordination.k8s.io]
    resources: [leases]
    verbs: [get, list, watch, create, update, patch, delete]
  - apiGroups: [rbac.authorization.k8s.io]
    resources: [roles, rolebindings]
    verbs: [get, list, watch, create, update, patch, delete]
  - apiGroups: [replica.nimeshbuilds.dev]
    resources: [clusterreplicas, replicaaccesses]
    verbs: [get, list, watch, update, patch]
  # Cleanup requests access revocation before it removes the guest API.
  - apiGroups: [replica.nimeshbuilds.dev]
    resources: [replicaaccesses]
    verbs: [delete]
  - apiGroups: [replica.nimeshbuilds.dev]
    resources: [clusterreplicas/status, replicaaccesses/status, clusterreplicas/finalizers, replicaaccesses/finalizers]
    verbs: [get, update, patch]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
  name: replicove-runtime
  namespace: replica-lab
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: replicove-runtime
subjects:
  - kind: ServiceAccount
    name: replicove
    namespace: replicove-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
  name: replicove-system-replicove-reader
rules:
  - apiGroups: [replica.nimeshbuilds.dev]
    resources: [replicagrants]
    verbs: [get]
  - apiGroups: [""]
    resources: [namespaces]
    resourceNames: [kube-system]
    verbs: [get]
  - apiGroups: [""]
    resources: [persistentvolumes]
    verbs: [get]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    replicove.nimeshbuilds.dev/infrastructure: "true"
  name: replicove-system-replicove-reader
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: replicove-system-replicove-reader
subjects:
  - kind: ServiceAccount
    name: replicove
    namespace: replicove-system
