# Kubernetes API reference

**[Read the generated API reference on the documentation site →](https://nimeshbuilds.github.io/replicove/reference/api/)**

The site builds the complete field reference directly from the [CRD schemas](../../config/crd/). This repository page is a pointer; edit the Go API comments/validation markers and run `make generate` to update the source of truth.
