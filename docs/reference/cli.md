# CLI reference

**[Read the generated CLI reference on the documentation site →](https://nimeshbuilds.github.io/replicove/reference/cli/)**

The site generates command and flag documentation from the built `bin/replicove --help` output. This repository page is a pointer; edit the Cobra command help in [cmd/replicove](../../cmd/replicove/) to update the source of truth.
