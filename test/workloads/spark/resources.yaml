# Adapted from Kubeflow spark-operator v2.5.2 examples/spark-pi.yaml.
# Copyright 2017 Google LLC. Licensed under Apache-2.0.
apiVersion: sparkoperator.k8s.io/v1beta2
kind: SparkApplication
metadata:
  name: spark-pi
  namespace: source-dev
spec:
  type: Scala
  mode: cluster
  image: docker.io/apache/spark:4.0.4
  imagePullPolicy: IfNotPresent
  mainClass: org.apache.spark.examples.SparkPi
  mainApplicationFile: local:///opt/spark/examples/jars/spark-examples.jar
  arguments: ["5"]
  sparkVersion: 4.0.4
  restartPolicy:
    type: Never
  sparkConf:
    spark.kubernetes.driver.request.cores: "100m"
    spark.kubernetes.executor.request.cores: "100m"
  driver:
    cores: 1
    memory: 512m
    serviceAccount: spark
    securityContext:
      capabilities: {drop: [ALL]}
      runAsGroup: 185
      runAsUser: 185
      runAsNonRoot: true
      allowPrivilegeEscalation: false
      seccompProfile: {type: RuntimeDefault}
  executor:
    instances: 1
    cores: 1
    memory: 512m
    securityContext:
      capabilities: {drop: [ALL]}
      runAsGroup: 185
      runAsUser: 185
      runAsNonRoot: true
      allowPrivilegeEscalation: false
      seccompProfile: {type: RuntimeDefault}
